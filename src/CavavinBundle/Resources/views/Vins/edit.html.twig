{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vins edition</h1>

    {{ form(edit_form) }}
    {{ dump(edit_form.nom) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('vins') }}">
            Retourner a la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
